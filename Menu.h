#ifndef MENU_H
#define MENU_H

/**
 * @brief Ficheiro Menu.h que contém as devidas funções declaradas que serão utilizadas no ficheiro .c
 * 
 */

void menu();
void GestaoClientes();
void GestaoGestores();
int compare(const void *a, const void *b);

#endif
